package webapp;
import Service.adapters.SecondaryAdapter.JSONManipulationAdapter;

import java.util.ArrayList;

import Model.Song;
import Model.users.*;
import Model.users.Playable.Basic;
import Model.users.Playable.IPlayable;
import Model.users.Playable.Premium;

public class App {
    public static void main(String[] args) throws Exception {
        // Song newHit = new Song("La cancion", "album", "Bad Bunny", 4);
        // IPlayable tipoBasico = new Basic();
        // IPlayable tipoPremium = new Premium();
        // User usuario = new User("username", "password", tipoBasico);
        // JSONManipulation.readJSON();
        // ArrayList<Song> canciones = new ArrayList();
        // canciones.add(newHit);
        // usuario.playSong(newHit);
        // usuario.setUserType(tipoPremium);
        // usuario.playSong(newHit);
        // usuario.createPlaylist("Mis favoritas", canciones, usuario.getUsername());
        // JSONManipulation.getInstanciedUsers().get(1).getPassword();
    }
}
